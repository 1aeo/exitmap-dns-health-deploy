# exitmap DNS Health Validation - Cron Configuration
#
# This file is a reference template. Use the install script to automatically
# configure cron from your config.env settings:
#
#   ./scripts/install-cron.sh           # Install cron entry
#   ./scripts/install-cron.sh --dry-run # Preview without installing
#
# The script reads these values from config.env:
#   - DEPLOY_PATH: Installation directory
#   - CRON_SCHEDULE: Cron timing (default: "15 */2 * * *")
#
# =============================================================================
# Manual Installation (alternative)
# =============================================================================
#
# User crontab (crontab -e):
#   15 */2 * * * /path/to/exitmap-dns-health-deploy/scripts/run-dns-validation.sh -c 4 >> /path/to/exitmap-dns-health-deploy/logs/cron.log 2>&1
#
# System cron.d (/etc/cron.d/exitmap-dns):
#   SHELL=/bin/bash
#   PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin
#   15 */2 * * * username /path/to/exitmap-dns-health-deploy/scripts/run-dns-validation.sh -c 4 >> /path/to/exitmap-dns-health-deploy/logs/cron.log 2>&1
#
# =============================================================================
# Schedule Options
# =============================================================================
#
# Every 2 hours (default):  15 */2 * * *
# Every 4 hours:            15 */4 * * *
# Every 6 hours:            15 */6 * * *
# Twice daily (6am/6pm):    15 6,18 * * *
# Once daily (3am):         15 3 * * *
